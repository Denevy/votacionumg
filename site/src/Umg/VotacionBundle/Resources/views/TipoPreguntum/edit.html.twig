{% extends '::base.html.twig' %}
{% block body -%}
<div class="row">
	<div class="col-md-12">
		<div class="panel panel-primary">
			<div class="panel-heading">
				<h1>Editar Tipo de Pregunta </h1>
			</div>
			<div class="panel-body">
				    {{ form(edit_form) }}
			        <a href="{{path('tipopreguntum') }}" class="btn btn-info fa fa-list"> Regresar</a>
			    {{ form(delete_form) }}
			</div>
		</div>
	</div>
</div>
{% endblock %}
