{% extends '::base.html.twig' %}
{% block body -%}
    <div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            Crear Campus
                        </div>
                        {{ form(form) }}
    					<a href="{{ path('campus') }}" class="fa fa-list btn btn-info"> Regresar </a>  
                        
                    </div>
                     <!-- End Form Elements -->
                </div>
</div>       
{% endblock %}

