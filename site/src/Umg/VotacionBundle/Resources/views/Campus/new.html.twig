{% extends '::base.html.twig' %}
{% block body -%}
    <div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            Crear Campus
                        </div>
                        {{ form(form) }}
    					<a href="{{ path('campus') }}" class="btn btn-primary"> Regresar </a>  
                        
                    </div>
                     <!-- End Form Elements -->
                </div>
</div>       
{% endblock %}

