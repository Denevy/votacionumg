{% extends '::base.html.twig' %}
{% block body -%}

<div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            {{ entity.Campus }}
                        </div>
                        <div class="panel-body">
                          	<div class="table table-striped table-bordered table-hover table-responsive">
        						{{ addDataTable(dataTable) }}
        					</div>  
                        </div>
                    </div>
                        <a href={{ path('campuscarrera_new', {'id': entity.id}) }} class="icon-code-fork btn btn-success"> Asiganar carrera a campus</a>
                        <a href="{{ path('campus') }}" class="icon-signout btn btn-info">Regresar</a>
                     <!-- End Form Elements -->
                </div>
</div>       
{% endblock %}
