{% extends '::base.html.twig' %}

{% block body -%}
<div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            <h1>Editar Catedrático</h1>
                        </div>
                        <div class="panel-body">
                            {{ form(edit_form) }} 
                            {{ form(delete_form) }}
                        </div>
                    </div>
                            <a href="{{ path('catedratico') }}"  class="icon-file-text-alt btn btn-info"> Regresar</a>
                     <!-- End Form Elements -->
                </div>
</div>
{% endblock %}
