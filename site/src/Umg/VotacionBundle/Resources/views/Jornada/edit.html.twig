{% extends '::base.html.twig' %}

{% block body -%}
<div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            Editar Jornada
                        </div>
                        <div class="panel-body">
                            {{ form(edit_form) }} 
                            <a href="{{ path('jornada') }}"  class="btn btn-primary"> Regresar</a>
                            {{ form(delete_form) }}
                        </div>
                    </div>
                     <!-- End Form Elements -->
                </div>
</div>
{% endblock %}
