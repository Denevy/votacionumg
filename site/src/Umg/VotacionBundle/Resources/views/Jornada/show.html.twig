{% extends '::base.html.twig' %}

{% block body -%}
<div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            Jornada
                        </div>
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered table-hover">
                                    <thead>
                                        <tr>
                                            <th>ID</th>
                                            <th>Nombre</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        <tr>
                                            <td>{{ entity.id }}</td>
                                            <td>{{ entity.Jornada }}</td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            {{ form(delete_form) }}
                        </div>
                    </div>
                            <a href="{{ path('jornada') }}" class="icon-signout btn btn-info"> Regresar</a>
                            <a href="{{ path('jornada_edit', { 'id': entity.id }) }}" class="icon-edit-sign btn btn-success" > Editar</a>
                     <!-- End Form Elements -->
                </div>
</div>
{% endblock %}

