{% extends '::base.html.twig' %}

{% block body -%}
<div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            <h1 class="page-title">Lista de evaluaciones</h1>
                        </div>
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered table-hover">
                                            <thead>
                                            <tr>
                                                <th>carrera</th>
                                                <th>Activa</th>
                                                <th>Fecha hora</th>
                                                <th>Fecha hora final</th>
                                                <th>Actiones</th>
                                            </tr>
                                        </thead>
                                    <tbody>
                                {% for entity in entities %}
                                    <tr>
                                        <td><a href="{{ path('evaluacion_show', { 'id': entity.id }) }}">{{ entity.CampusCarrera }}</a></td>
                                        <td>{% if  entity.Activa ==  1 %}Sí {% else %} No {% endif %}</td>
                                        <td>{% if entity.FechaHora %}{{ entity.FechaHora|date('Y-m-d H:i:s') }}{% endif %}</td>
                                        <td>{% if entity.FechaHoraFinal %}{{ entity.FechaHoraFinal|date('Y-m-d H:i:s') }}{% endif %}</td>
                                        <td>
                                        <ul>
                                            <li>
                                                <a href="{{ path('evaluacion_show', { 'id': entity.id }) }}">ver</a>
                                            </li>
                                            <li>
                                                <a href="{{ path('evaluacion_edit', { 'id': entity.id }) }}">editar</a>
                                            </li>
                                        </ul>
                                        </td>
                                    </tr>
                                {% endfor %}
                                </tbody>
                                </table>
                            </div>
                            <a href="{{ path('evaluacion_new') }}" class="btn btn-primary">
                            Nueva encuesta
                            </a>
                        </div>
                    </div>
                     <!-- End Form Elements -->
                </div>
</div>
{% endblock %}
