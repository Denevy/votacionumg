{% extends '::base.html.twig' %}
{% block body -%}
    <div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            Editar asignación de carrera a campus
                        </div>
                        {{ form_javascript(edit_form) }}
                        {{ form(edit_form) }}
    					<a href="{{ path('campus_show', {id:entity.Campus.id}) }}" class="fa fa-list btn btn-info"> Regresar a Campus </a>  
                        
                    </div>
                     <!-- End Form Elements -->
                </div>
</div>       
{% endblock %}
