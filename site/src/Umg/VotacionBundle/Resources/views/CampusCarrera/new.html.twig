{% extends '::base.html.twig' %}
{% block body -%}
    <div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            Asignación de Carrera a Campus
                        </div>
                        {{ form_javascript(form) }}
                        {{ form(form) }}
    					<a href="{{ path('campus_show', {id:entity.Campus.id}) }}" class="fa fa-list btn btn-info"> Regresar a Campus </a>  
                        
                    </div>
                     <!-- End Form Elements -->
                </div>
</div>       
{% endblock %}

