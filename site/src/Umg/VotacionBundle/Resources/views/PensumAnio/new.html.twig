{% extends '::base.html.twig' %}
{% block body -%}
    <div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            Asignación de Curso a Pensum
                        </div>
                        {{ form(form) }}
                        
                    </div>
    					<a href="{{  path('pensum', {'id': entity.Pensum.id}) }}" class="icon-signout btn btn-info"> Regresar al pensum</a>  
                     <!-- End Form Elements -->
                </div>
</div>       
{% endblock %}


