{% extends '::base.html.twig' %}
{% block body -%}
<div class="row">
                <div class="col-md-12">
                    <!-- Form Elements -->
                    <div class="panel panel-primary">
                        <div class="panel-heading">
                            <h1 class="page-title">{{ entity.Curso }}</h1>
                            <p class="lead">Código: {{entity.Codigo}} Carrera: {{entity.Carrera}}</p>
                        </div>
                        <div class="panel-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-bordered table-hover">
                                    <tbody>
                                        <tr>
                                            <th>Pensum</th>
                                            <td>{{ entity.Pensum }}</td>
                                        </tr>
                                    </tbody>
                                </table>
                            </div>
                            {{ form(delete_form) }}
                        </div>
                    </div>
                            <a href="{{ path('pensum_show', { 'id': entity.Carrera.id }) }}" class="icon-signout btn btn-info"> Regresar a la lista</a>
                            <a href="{{  path('pensumanio_edit', { 'id': entity.id }) }}" class="icon-edit-sign btn btn-success" > Editar</a>
                     <!-- End Form Elements -->
                </div>
</div>
{% endblock %}

